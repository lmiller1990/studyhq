// import { FreeTrialExpiredError } from "~/logic/errors";

export async function tryDeductFreeMessage() {
  //   const rem = Number(
  //     await db("users")
  //       .where({ id: user.id })
  //       .update({
  //         credit: user.credit - 1,
  //       })
  //       .returning(""),
  //   );
  //   if (rem < 0) {
  //     throw new FreeTrialExpiredError();
  //   }
}
